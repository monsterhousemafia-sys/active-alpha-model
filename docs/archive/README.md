# Historical phase reports (archive index)

Sealed external review documents (`EXTERNAL_REVIEW_APPROVAL_*.md`) and their phase reports (`CODEX_*_*.md`, `CODEX_*_REPORT.md`) remain in the **repository root** by design — they are referenced by `review_registry.json` and review ZIP manifests.

Do **not** move or rewrite sealed approval files.

Optional future cleanup: copy non-authoritative duplicate preflight drafts here with a manifest; never alter hashes referenced by the review chain.
